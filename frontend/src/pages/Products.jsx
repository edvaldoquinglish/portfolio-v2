export default function Products() {
  return (
    <section className="section">
      <div className="container">
        <h1 className="section-title">Products</h1>
        <p className="section-subtitle">Catálogo de produtos (SaaS, Apps, Tools, etc.).</p>
        <div className="card">
          <p style={{ color: 'var(--text-muted)' }}>
            Placeholder — dados de DEMO via API. Não inventar produtos reais.
          </p>
        </div>
      </div>
    </section>
  )
}
